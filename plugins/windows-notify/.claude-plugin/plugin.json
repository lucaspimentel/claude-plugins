{
  "name": "windows-notify",
  "description": "Shows Windows toast notifications when Claude Code needs attention",
  "author": { "name": "Lucas Pimentel" },
  "repository": "https://github.com/lucaspimentel/claude-plugins",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if [ \"$OS\" = \"Windows_NT\" ]; then powershell -NoProfile -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/scripts/register.ps1\"; else echo '{\"systemMessage\":\"[windows-notify] This plugin only works on Windows. Notifications will not be shown.\"}'; fi"
          }
        ]
      }
    ],
    "Notification": [
      {
        "matcher": "permission_prompt|idle_prompt",
        "hooks": [
          {
            "type": "command",
            "command": "[ \"$OS\" = \"Windows_NT\" ] && powershell -NoProfile -ExecutionPolicy Bypass -File \"${CLAUDE_PLUGIN_ROOT}/scripts/notify.ps1\""
          }
        ]
      }
    ]
  }
}
